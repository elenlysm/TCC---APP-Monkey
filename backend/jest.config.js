module.exports = {
    testEnvironment: 'node', //Define que o ambiente de teste será Node.js (não browser)
    verbose: true, //Ativa a exibição detalhada dos testes durante a execução
    testMatch: ['**/_tests_/**/*.test.js'], //Define o padrão de arquivos que serão considerados testes
};
